= form_for @group do |f|
  - if @group.errors.any?
    .group-form__errors
      %h2= "#{@group.errors.full_messages.count}件のエラーが発生しました。"
      %ul
        - @group.errors.full_messages.each do |message|
          %li= message

  .group-form__field--right
    %label.group-form__label{:for => "group_カテゴリを追加"} カテゴリを追加
    = f.text_field :name, class: 'group-form__input', placeholder: 'カテゴリ名を入力してください'


  .group-form__field.clearfix
    .group-form__field--left
    .group-form__field--right
      = f.submit class: 'group-form__action-btn'