.main-container
  - @posts.each do |post|
    = link_to post_path(post.id), class: "top-link" do
      .post-image
        = post.image
      .post-title
        = post.title
      .post-user
        %p 投稿者：
        .post-user__name
        = post.user.name
